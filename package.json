{
  "name": "js-class",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prestart": "npm run clean",
    "prebuild": "npm run clean",
    "clean": "npx del cnit133",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "eleventy && prettier --write cnit133",
    "watch": "eleventy --watch",
    "serve": "eleventy --serve",
    "start": "eleventy --serve",
    "debug": "DEBUG=* eleventy",
    "pretty": "prettier --write _site",
    "publish": "scp -r cnit133 wpuckett@hills.ccsf.edu:/students/wpuckett/public_html"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^0.12.1",
    "del-cli": "^4.0.1",
    "prettier": "^2.3.2"
  },
  "dependencies": {
    "highlight.js": "^11.2.0"
  }
}
